{
  "name": "meme-launchpad-evm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npx hardhat test",
    "node": "npx hardhat node",
    "deploy": "node --no-warnings scripts/deploy.js"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^3.0.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.16.0",
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.0.0"
  }
}
